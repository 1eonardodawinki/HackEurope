# ════════════════════════════════════════════════════════
#  Maritime Sentinel — Environment Variables
# ════════════════════════════════════════════════════════

# ── REQUIRED ─────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── FRONTEND (also create frontend/.env with this) ───────
VITE_MAPBOX_TOKEN=

# ── LIVE AIS DATA ─────────────────────────────────────────
# Get a free key at https://aisstream.io
# If not set → DEMO_MODE auto-enables with simulated ships
AISSTREAM_API_KEY=

# ── SUPABASE (persistence) ───────────────────────────────
# 1. Create free project at https://supabase.com
# 2. Run supabase/schema.sql in the SQL editor
# 3. Copy your project URL + service role key here
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# ── GFW (1-year vessel path) ───────────────────────────────
# Get token at https://globalfishingwatch.org/our-apis/tokens
GFW_API_TOKEN=

# ── OPTIONAL enrichment ───────────────────────────────────
NEWS_API_KEY=        # https://newsapi.org (free tier)
ALPHA_VANTAGE_KEY=   # https://alphavantage.co (free tier)

# ── DEMO MODE ─────────────────────────────────────────────
# Auto-set: true when AISSTREAM_API_KEY missing, false when present
# Override: DEMO_MODE=true  (simulated ships, real Claude agents)
#           DEMO_MODE=false (live AIS feed — requires AISSTREAM_API_KEY)
