fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-dotenv>=1.0.0
anthropic>=0.40.0
httpx>=0.26.0
pydantic>=2.5.0
aiohttp>=3.9.0
reportlab>=4.0.0
pillow>=9.0.0
# Supabase is accessed via httpx (REST/PostgREST) — no Python SDK needed
# This avoids the pyiceberg/pyroaring build failures on Python 3.14/Windows
